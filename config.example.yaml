interval: 300

log:
  level: debug
  format: simple

zones:
  - name: somedomain.com
    provider: cloudflare
    apiToken: ...
    zoneID: ...

dns:
  a: true
  aaaa: false
  defaultTTL: 300
  purgeUnknown: true

domains:
  - name: "*.somedomain.com"
    a: 10.0.0.2

  - name: "somedomain.com"
    a: 10.0.0.2
